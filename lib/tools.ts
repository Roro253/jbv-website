export const toolHandlers: Record<string, (args: unknown) => Promise<string>> = {
  // Example stub handlers for future extension:
  // async function getAirtableRows(args) { ... }
  // "getAirtableRows": async (args) => JSON.stringify(await getAirtableRows(args)),
};

